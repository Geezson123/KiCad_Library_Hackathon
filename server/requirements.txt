Flask>=3.0
kiutils>=1.4.8
# Optional: only needed for AI enrichment on Mouser ingest. server/ai.py imports it
# lazily and degrades gracefully, so the app runs fine without it installed.
anthropic>=0.69
gunicorn>=21.2 ; sys_platform != "win32"
